<!DOCTYPE html>
<html lang="zh-TW">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="description" content="國立暨南國際大學遺失物系統">
    <title>物品遺失</title>
    <% include ../partials/bootstrap_css %>
</head>

<body>
    <% include ../partials/navBar %>
    <% include ../partials/add %>
    <!-- 下面的inclue若出錯可放回container下方，
    放上來，是因為，loading順序 -->
    <% include ../partials/bootstrap_js %>
    <% include ../partials/vue_js %>
    <% include ../partials/add %>
    <% include ../partials/categories %>
    <div class="container">
        <h1>東西不見</h1>
        
        <div id="lost">
            <form class="mb-3">
                <div class="form-group">
                    <label for="name">請輸入物品名稱</label>
                    <input type="text" class="form-control" id="name" v-model.trim="name" placeholder="物品名稱...">
                </div>
                <div class="form-group">
                    <label for="location">請輸入拾獲地點</label>
                    <input type="text" class="form-control" id="location" v-model.trim="location" placeholder="拾獲地點...">
                </div>
                <div class="form-group">
                    <label for="description">簡單描述物品(外觀、顏色、特徵)</label>
                    <input type="text" class="form-control" id="description" v-model.trim="description" placeholder="物品描述...">
                </div>
                <div class="form-group">
                    <label for="fication">物品分類</label>
                    <select class="form-control" id="fication" v-model.number="fication">
                        <option disabled value="">請選擇</option>
                        <option value="1">衣物</option>
                        <option value="2">現金</option>
                        <option value="3">書籍</option>
                        <option value="4">3C</option>
                        <option value="5">文具</option>
                        <option value="6">卡片</option>
                        <option value="7">雨具</option>
                        <option value="8">生活</option>
                        <option value="9">其他</option>
                    </select>
                </div>
                <div v-for="(value, key) in showFormData">
                    {{ key }}: {{ value }}
                </div>
                <button @click.prevent="createFound" class="btn btn-primary">送出</button>
            </form>


            <div class="row" v-if="lists.length!=0">
                <div class="col-12" v-for="list in lists">
                    <div class="row border rounded mb-2 p-2">
                        <div class="col-md-3 mb-2 mb-md-0">
                            <img class="border rounded img-fluid" src="http://media.viralcham.com/wp-content/uploads/2017/08/10124047/ecoforestwbbrevjs5.png" alt="Card image cap">
                        </div>
                        <div class="col-md-9">
                            <h4 class="card-title">{{ list.name }}</h4>
                            <p class="card-text text-truncate">{{ textIsEmpty(list.description) }}</p>
                            <div class="text-right">
                                <a href="#" class="btn btn-primary">看更多</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div v-else>
                <div class="alert alert-info">
                    目前還沒有遺失物喔
                </div>
            </div>
        </div>
    </div>
    <script>
        var app = new Vue({
            el: '#lost',
            data: {
                lists: '',
                name: '',
                location: '',
                description: '',
                fication: '',
            },
            // 網頁載入完成
            created: function () {
                var self = this;
                axios.get('/api/lost/')
                    .then(function (response) {
                        console.log(response.data);
                        self.lists = response.data;
                    })
                    .catch(function (error) {
                        console.log(error);
                        // 待加入跳轉到404頁面
                    });
            },
            methods: {
                textIsEmpty (text) {
                    return text == null ? "空空如也" : text;
                },
                objIsEmpty (obj) {
                    let tmp = 1;
                    for (let key in obj) {
                        if (obj[key] == '') {
                            tmp = 0;
                        }
                    }
                    return tmp;
                },
                createFound () {
                    if(this.objIsEmpty(this.showFormData)){
                        console.log("有東西");
                        axios.post('/api/lost', {
                            "name": this.name,
                            "classification_id": this.fication,
                            "location": this.location,
                            "time_interval_LB": "2017-03-31T10:00:00.000Z",
                            "time_interval_UB": "2017-03-31T16:00:00.000Z",
                            "description": this.description
                        })
                        .then(function (response) {
                            console.log(response);
                        })
                        .catch(function (error) {
                            console.log(error);
                        });
                    }else {
                        console.log("有東西沒填");
                    }
                }
            },
            computed: {
                showFormData () {
                    return {
                        name: this.name,
                        location: this.location,
                        description: this.description,
                        fication: this.fication
                    }
                }
            }
        })
    </script>
</body>

</html>